function moveNoButton() {
    var button = document.getElementById("no-button");
    button.style.position = "absolute";
    button.style.left = Math.random() * 90 + "%";
    button.style.top = Math.random() * 90 + "%";
}

function openGif() {
    window.open("https://makeagif.com/i/luyI9E");
}